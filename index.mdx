---
title: "Powerlevel10k: Fast, Flexible Zsh Theme with Instant Prompt"
sidebarTitle: "Home"
description: "Powerlevel10k is a highly customizable Zsh theme with instant prompt, 60+ built-in segments, and an interactive configuration wizard — zero prompt lag."
---

Powerlevel10k is a theme for Zsh that emphasizes speed, flexibility, and a great out-of-the-box experience. It delivers an instant prompt the moment your terminal opens, a built-in interactive configuration wizard, and over 60 prompt segments covering everything from Git status to Kubernetes contexts — all without any perceptible latency.

<CardGroup cols={2}>
  <Card title="Installation" icon="download" href="/installation">
    Install Powerlevel10k manually or through your favourite Zsh plugin manager in minutes.
  </Card>
  <Card title="Configuration Wizard" icon="wand-magic-sparkles" href="/configuration/wizard">
    Run `p10k configure` to pick your prompt style interactively — no file editing required.
  </Card>
  <Card title="Prompt Segments" icon="puzzle-piece" href="/configuration/prompt-segments">
    Browse all 60+ built-in segments and learn how to enable, disable, and reorder them.
  </Card>
  <Card title="Custom Segments" icon="code" href="/configuration/custom-segments">
    Build your own prompt segments with the public `p10k segment` API.
  </Card>
  <Card title="Instant Prompt" icon="bolt" href="/configuration/instant-prompt">
    Eliminate Zsh startup lag and display your prompt before plugins finish loading.
  </Card>
  <Card title="Powerlevel9k Migration" icon="arrow-right-arrow-left" href="/compatibility/powerlevel9k">
    Migrate from Powerlevel9k in one command — all your config parameters work as-is.
  </Card>
</CardGroup>

## Get Up and Running

<Steps>
  <Step title="Install the recommended font (optional but recommended)">
    Download and install [MesloLGS NF](/fonts) to unlock all prompt styles and icons.
  </Step>
  <Step title="Install Powerlevel10k">
    Clone the repository and source the theme from your `~/.zshrc`:
    ```zsh
    git clone --depth=1 https://github.com/romkatv/powerlevel10k.git ~/powerlevel10k
    echo 'source ~/powerlevel10k/powerlevel10k.zsh-theme' >>~/.zshrc
    ```
    See the [full installation guide](/installation) for plugin-manager options.
  </Step>
  <Step title="Restart Zsh">
    ```zsh
    exec zsh
    ```
  </Step>
  <Step title="Run the configuration wizard">
    The wizard starts automatically on first run. You can re-run it any time:
    ```zsh
    p10k configure
    ```
  </Step>
</Steps>

## Why Powerlevel10k?

<CardGroup cols={3}>
  <Card title="Zero Prompt Lag" icon="gauge-high">
    Every keystroke produces an instant response. Powerlevel10k uses asynchronous workers and a native `gitstatusd` daemon so complex prompt segments never block the shell.
  </Card>
  <Card title="Interactive Setup" icon="sliders">
    The built-in wizard walks you through every visual choice and writes a fully-commented `~/.p10k.zsh` you can continue to tweak by hand.
  </Card>
  <Card title="60+ Segments" icon="layer-group">
    Git, AWS, Kubernetes, Python, Node.js, Terraform, battery, time, and many more — all configurable, all fast.
  </Card>
</CardGroup>

<Note>
  Powerlevel10k is in **limited-support mode**. No new features are being developed, most bugs will go unfixed, and help requests may not receive a response. See the [GitHub repository](https://github.com/romkatv/powerlevel10k) for details.
</Note>
